# Used by docker-compose.yml. Copy this file to ".env" (same folder as
# docker-compose.yml) and fill in real values before running `docker compose up`.

MYSQL_ROOT_PASSWORD=change-me-to-a-strong-password
JWT_SECRET=change-me-to-a-random-string-at-least-32-characters-long
